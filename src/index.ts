import { isIdCardNo } from './utils/isIdCardNo'
import { throttle } from './utils/throttle'
import { debounce } from './utils/debounce'

const chest = {
  debounce,
  throttle,
  isIdCardNo,
}

export default chest
